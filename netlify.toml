[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# SPA redirect: all routes that are not API or static files go to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
